{
	"name": "express",
	"version": "1.0.0",
	"license": "MIT",
	"author": "Sam Larsen <sam@slarsen.io>",
	"type": "module",
	"scripts": {
		"start": "node src/server.js",
		"lint": "prettier --use-tabs --trailing-comma 'none' --check .",
		"format": "prettier --use-tabs --trailing-comma 'none' --write ."
	},
	"dependencies": {
		"express": "^4.18.2",
		"openai": "^4.12.4"
	},
	"devDependencies": {
		"dotenv": "^16.3.1",
		"prettier": "^3.0.3"
	}
}
